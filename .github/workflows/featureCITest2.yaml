name: FeatureCI2

on:
  push:
    branch:
      - 'feature/**'

jobs:
  RunKarateTests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build and run Karate tests
        run: |
          mvn clean test